import logging

logging.basicConfig(
    level=logging.DEBUG,
    format='%(asctime)s - %(levelname)s - %(message)s'
)

logging.debug("log nivel debug")
logging.info("log nivel debug")
logging.warning("log nivel debug")
logging.error("log nivel debug")